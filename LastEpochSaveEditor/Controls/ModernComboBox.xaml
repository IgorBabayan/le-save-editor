<UserControl x:Class="LastEpochSaveEditor.Controls.ModernComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LastEpochSaveEditor.Controls"
             Height="22" Width="22">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<ToggleButton Grid.Row="0" x:Name="MainButton">
			<ContentPresenter />
		</ToggleButton>

		<Popup Grid.Row="2" AllowsTransparency="True"
			   Placement="Bottom" HorizontalOffset="-30"
			   IsOpen="{Binding ElementName=MainButton, Path=IsChecked, Mode=OneWay}"
			   PlacementTarget="{Binding ElementName=MainButton}">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition />
				</Grid.RowDefinitions>

				<Path Grid.Row="0" Style="{StaticResource ArrowPathDown}" />

				<Border Grid.Row="1" CornerRadius="4" Background="White">
					<ItemsControl x:Name="ItemsControl" ItemsSource="{Binding ItemsSource}"
								  DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:ModernComboBox}}}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<Button Margin="10" Content="My button" />
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</Border>
			</Grid>
		</Popup>
	</Grid>
</UserControl>
