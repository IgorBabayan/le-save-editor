<Button x:Class="LastEpochSaveEditor.Controls.ItemButton"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:LastEpochSaveEditor.Controls"
        xmlns:converters="clr-namespace:LastEpochSaveEditor.Converters"
        Style="{DynamicResource ImageButton}">
	<Button.Resources>
		<converters:QualityToColorConverter x:Key="QualityToColorConverter"></converters:QualityToColorConverter>
	</Button.Resources>
	<Border BorderBrush="{Binding Item.Quality, Converter={StaticResource QualityToColorConverter}}"
			BorderThickness="1"
	        DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:ItemButton}}}">
		<controls:ItemImage Item="{Binding Item}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
		                    Width="{Binding Width}" Height="{Binding Height}"
		                    Margin="1" />
	</Border>
	
</Button>
